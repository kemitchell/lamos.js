{
  "name": "lamos",
  "description": "serialize lists and maps of strings",
  "version": "1.0.4",
  "author": "Kyle E. Mitchell <kyle@kemitchell.com> (https://kemitchell.com/)",
  "bin": {
    "lamos-to-json": "./lamos-to-json",
    "json-to-lamos": "./json-to-lamos"
  },
  "dependencies": {
    "flush-write-stream": "^1.0.2",
    "pump": "^1.0.2",
    "pumpify": "^1.3.5",
    "split2": "^2.1.1",
    "string-to-stream": "^1.1.0",
    "through2": "^2.0.3"
  },
  "devDependencies": {
    "concat-stream": "^1.6.0",
    "defence-cli": "^2.0.1",
    "depcheck": "^0.6.7",
    "istanbul": "^0.4.5",
    "replace-require-self": "^1.1.1",
    "standard": "^10.0.2",
    "standard-markdown": "^2.3.0",
    "tape": "^4.6.3"
  },
  "license": "MIT",
  "repository": "kemitchell/lamos.js",
  "scripts": {
    "coverage": "istanbul check-coverage --statements 100 --functions 100 --branches 100 --lines 100",
    "lint": "standard && standard-markdown",
    "precoverage": "istanbul cover -- test.js",
    "prepublish": "depcheck",
    "test": "node test.js && defence -i javascript < README.md | replace-require-self | node"
  }
}
